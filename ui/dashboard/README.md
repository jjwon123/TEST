# Dashboard UI

Shows run list, current stage, locked stages, approval status, and recent errors.

The dashboard should read `runs/{run-id}/run-status.json` and `approvals.json` without inventing separate state.
