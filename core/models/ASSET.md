# Asset Model

An asset is any visual, export, or reusable source file tracked by the pipeline. Assets should keep source path, usage, channel, QA status, reuse status, and tags.

Selected and final assets can be promoted into `assets/reusable/` only after QA approval.
