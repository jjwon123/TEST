# Brand Model

A brand model defines voice, visual principles, required phrases, banned words, and safety constraints used across every stage.

The brand guide is read during brief normalization and remains a constraint for content planning, visual candidate generation, assembly, QA, and archive tagging.
