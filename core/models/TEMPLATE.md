# Template Model

A template is a reusable Figma layout contract. It defines where text and visuals can go and how the output should be exported.

Templates are metadata first. Figma automation should read this metadata rather than guessing from layer names.
