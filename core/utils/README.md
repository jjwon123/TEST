# Utils

Shared utilities should live here only when they are independent of a specific service or stage. Stage-specific logic belongs in `pipeline/{stage}/handlers/`; external integration code belongs in `services/`.
