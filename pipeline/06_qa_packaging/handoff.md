# Handoff

`07_asset_archive` receives the final package manifest and QA report. Archive should only store publishable outputs and reusable source assets.

## Required Handoff Fields

- `package_id`
- `outputs[]`
- `qa_status`
- `accepted_warnings`
- `revision_items`
- `final_paths`
