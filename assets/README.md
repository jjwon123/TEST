# Assets

This folder stores cross-run asset collections.

- `approved/`: approved final or source assets
- `rejected/`: rejected assets retained for audit
- `reusable/`: assets promoted for future events
- `exports/`: shared exported files
- `indexes/`: searchable asset index files

Run-specific assets should remain under `runs/{run-id}/` until they are explicitly promoted.
